import 'package:flutter/material.dart';

class DoublesGenerator extends StatelessWidget {
  const DoublesGenerator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
